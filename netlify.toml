[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback — todas as rotas do React Router caem no index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
